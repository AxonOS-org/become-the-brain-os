# Release Notes — v12.5.1-evolver

## Status

Cleanup and playability release.

## Fixed

- Broken release surface drift from old `v9.*` and `v12.1.0` assets.
- Impossible / unclear gameplay contract.
- Decorative disabled-state on hold control.
- Pointer-capture edge cases.
- Missing safe fallback on cancel / blur / timeout.
- Mobile overflow and unstable card layout.
- CI one-line YAML failure pattern.
- Validator self-matching stale-version errors.

## Non-claim

This is a local-only educational simulation. It is not medical software, not a BCI, and reads no neural data.
