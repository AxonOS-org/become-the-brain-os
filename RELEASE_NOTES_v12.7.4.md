# AxonOS Evolver v12.7.4-evolver

## Status

Emergency playability and black-screen prevention release.

## Key fixes

- Static HTML renders visible content before JavaScript runs.
- Runtime is wrapped in a visible error guard.
- No blank-screen failure mode: errors appear in a red runtime guard panel.
- Simplified and stabilized gameplay.
- Real hold button uses `<button disabled>` state.
- Choice timeout uses safe fallback, not hard failure.
- Local-only storage namespace: `axonos_v1274_`.
- Challenge prefix: `v1274`.

## Non-claim

Educational simulation only. No neural data. No backend. No analytics.
